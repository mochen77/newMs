var express = require('express');
var router = express.Router();

/* 添加陆地动物信息 */
//http://localhost:3000/terricole/add
// router.post("/add", upload.single("logo"), TerricoleService.add);

module.exports = router;